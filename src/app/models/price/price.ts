export class Price {
  food : number;
  delivery : number;
  tip : number;

  constructor(food: number, delivery: number, tip: number) {
    this.food = food;
    this.delivery = delivery;
    this.tip = tip;
  }
}
